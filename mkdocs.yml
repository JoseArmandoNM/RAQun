site_name: RAQun

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          options:
            docstring_style: numpy

nav:
  - Home: index.md
  - API:
      - Algorithms: api/algorithms.md
      - Circuits: api/circuits.md
      - Utils: api/utils.md