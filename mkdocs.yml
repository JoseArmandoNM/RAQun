site_name: RAQun

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          options:
            docstring_style: numpy

nav:
  - Home: index.md
  - API:
      - Algorithm: api/algorithm.md
      - Circuit: api/circuit.md